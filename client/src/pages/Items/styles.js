const styles = theme => ({
    background: {
        height: '100%',
        width: '100%',
        backgroundColor: theme.palette.secondary.main,
        paddingTop:100
    },
});

export default styles;
